# 캘린더 뷰 설계

## 1. 개요

HomeScreen 내에서 리스트 뷰와 캘린더 뷰를 토글하여 전환하는 기능.

## 2. 화면 구성

### 2.1 캘린더 뷰 레이아웃

```
┌────────────────────────────────────┐
│  [리스트] [캘린더]  ← 토글 버튼     │
├────────────────────────────────────┤
│  ◀  2026년 2월  ▶                  │
│  수입: +100,000  지출: -15,000     │
│  잔액: +85,000                     │
├────────────────────────────────────┤
│ 일  월  화  수  목  금  토          │
├────────────────────────────────────┤
│                         1          │
│                      +5,000        │
├────────────────────────────────────┤
│  2    3    4    5    6    7    8   │
│     -2k  +10k -1.5k           -3k  │
├────────────────────────────────────┤
│  ...                               │
└────────────────────────────────────┘
```

### 2.2 날짜 선택 모달

```
┌────────────────────────────────────┐
│  2월 4일 (화)                  X   │
├────────────────────────────────────┤
│  🍔 식비        -8,000원   신용카드 │
│     점심 식사                       │
├────────────────────────────────────┤
│  💰 급여      +100,000원   계좌이체 │
├────────────────────────────────────┤
│  ☕ 카페        -5,000원   현금     │
│     아메리카노                      │
├────────────────────────────────────┤
│  일일 합계: +87,000원              │
└────────────────────────────────────┘
```

## 3. 컴포넌트 구조

```
HomeScreen
├── ViewToggle (리스트/캘린더 전환)
├── SummaryCard (공통)
│
├── [리스트 뷰]
│   ├── FilterButtons
│   └── TransactionList
│
└── [캘린더 뷰]
    ├── CalendarHeader (월 네비게이션)
    ├── CalendarGrid (달력 그리드)
    │   └── DayCell (각 날짜 셀)
    └── DayDetailModal (날짜 선택 모달)
```

## 4. 컴포넌트 명세

### 4.1 ViewToggle
- Props: `view: 'list' | 'calendar'`, `onViewChange`
- 기능: 리스트/캘린더 전환 버튼

### 4.2 CalendarHeader
- Props: `year`, `month`, `onPrevMonth`, `onNextMonth`
- 기능: 월 표시, 이전/다음 월 네비게이션

### 4.3 CalendarGrid
- Props: `year`, `month`, `dailySummaries`, `onDayPress`
- 기능: 달력 그리드 렌더링

### 4.4 DayCell
- Props: `day`, `summary: { income, expense, balance }`, `isToday`, `onPress`
- 기능:
  - 날짜 표시
  - 일일 잔액 표시 (양수: 초록, 음수: 빨강)
  - 오늘 날짜 하이라이트

### 4.5 DayDetailModal
- Props: `visible`, `date`, `transactions`, `onClose`
- 기능:
  - 해당 일의 거래 목록 표시
  - 일일 합계 표시
  - 닫기 버튼

## 5. 데이터 구조

### 5.1 DailySummary
```typescript
interface DailySummary {
  date: string;           // 'YYYY-MM-DD'
  totalIncome: number;
  totalExpense: number;
  balance: number;        // income - expense
  transactionCount: number;
}
```

### 5.2 TransactionService 추가 메서드
```typescript
interface ITransactionService {
  // 기존 메서드...

  // 추가
  getDailySummaries(year: number, month: number): DailySummary[];
  getByDate(date: Date): Transaction[];
}
```

## 6. 상태 관리

```typescript
// HomeScreen 상태
const [viewMode, setViewMode] = useState<'list' | 'calendar'>('list');
const [currentYear, setCurrentYear] = useState(new Date().getFullYear());
const [currentMonth, setCurrentMonth] = useState(new Date().getMonth() + 1);
const [selectedDate, setSelectedDate] = useState<Date | null>(null);
const [modalVisible, setModalVisible] = useState(false);
```

## 7. 스타일 가이드

### 7.1 색상
- 수입 (양수): `#4CAF50` (초록)
- 지출 (음수): `#F44336` (빨강)
- 오늘 날짜: `#2196F3` 배경 (파랑)
- 선택된 날짜: `#E3F2FD` 배경 (연파랑)

### 7.2 금액 표시
- 1000 이상: `+10k`, `-5k` (축약)
- 1000 미만: `+500`, `-200` (전체)

## 8. 구현 순서

1. TransactionService에 `getDailySummaries`, `getByDate` 추가
2. ViewToggle 컴포넌트
3. CalendarHeader 컴포넌트
4. DayCell 컴포넌트
5. CalendarGrid 컴포넌트
6. DayDetailModal 컴포넌트
7. HomeScreen에 캘린더 뷰 통합
8. 테스트 코드 작성
